services:
  - type: web
    name: telegram-auto-login
    env: python
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y chromium chromium-driver
      pip install -r requirements.txt
    startCommand: python auto_login_telegram.py
